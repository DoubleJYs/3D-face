# Separate V15 statistical companion

The anonymous V15 frozen-results package remains separate from this minimum
software archive:

```text
paper_rewriting_output/final_paper_w49n_v15_zh_working/anonymous_evaluation_package_v15
```

At the last completed validation, its `SHA256SUMS.txt` had SHA-256
`5701ea5bfa8606f47f9f7b202ff8e5fd111820877a36918497aeb987510bedfb`.
That value is a local binding, not a DOI or permanent release identifier.

The companion reconstructs the frozen identity-level comparisons, bootstrap
intervals, exact sign tests, Holm families, and directional summaries. It also
contains manuscript mappings and figures, so it is deliberately not copied
into this source-only archive. A future publication may release it as a second
archive under the same version record.
